apiVersion: v1
kind: Secret
metadata:
  name: persons-finder-openai
  namespace: persons-finder-dev
type: Opaque
stringData:
  # Set this before applying, or use: kubectl -n persons-finder-dev create secret generic persons-finder-openai --from-literal=OPENAI_API_KEY='...'
  OPENAI_API_KEY: "<REPLACE_ME>"
